import 'package:flutter/material.dart';

class AddDepScreen extends StatelessWidget {
  const AddDepScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
